---
project_name:         altitudelabs-cms                      # You repo will be install in this folder, NO-SPACE-ALLOWED
keypair:              altitudelabs                  # Key name to access the instance (from ~/.ssh/)
nginx_server_port:    3000                          # Port number to start the server
git_path:             altitudelabs/altitudelabs-cms  # Github repo
git_production_branch:   master
git_staging_branch:   master
app_js:               keystone.js
deploy_sh_file:       deploy.sh
vm_dependencies:
  nodejs:
    version: '4.4.1'
  npm:
    - gulp
    - bower
  mongodb:
